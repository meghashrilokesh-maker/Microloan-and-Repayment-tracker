{
  "name": "microloan-and-repayment-tracker",
  "version": "1.0.0",
  "description": "A societal project to help small vendors manage microloans, repayments, daily sales, and expenses.",
  "private": true,
  "type": "module",
  "scripts": {
    "server:dev": "npm --prefix server run dev",
    "server:start": "npm --prefix server start",
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "build": "npm --prefix server run db:generate && npm --prefix client run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "keywords": [
    "microloan",
    "repayment-tracker",
    "street-vendors",
    "financial-inclusion"
  ],
  "author": "",
  "license": "ISC"
}

